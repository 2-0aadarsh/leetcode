class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        set<int> st(nums.begin(), nums.end());
        int x = st.size();
        int y = nums.size();
        return (y>x);
    }
};
